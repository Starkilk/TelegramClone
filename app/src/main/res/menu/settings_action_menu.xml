<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--Settings action menu-->
    <item android:icon="@drawable/ic_b_action_menu"
        app:showAsAction="ifRoom"
        android:title="">
        <menu>
            <item android:id="@+id/settingsMenuChangeName"
                android:title="@string/settings_menu_edit_name"
                android:icon="@drawable/ic_b_action_menu_edit_name"
                app:showAsAction="never"/>

            <item android:id="@+id/settingsMenuExit"
                android:title="@string/settings_menu_exit"
                android:icon="@drawable/ic_b_action_menu_exit"
                app:showAsAction="never"/>
        </menu>
    </item>





</menu>