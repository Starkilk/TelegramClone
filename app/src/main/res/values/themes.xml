<resources xmlns:tools="http://schemas.android.com/tools">

    <!--Style for menu-->
    <style name="actionMenu" parent="Theme.AppCompat.Light">
        <item name="background">@color/black</item>
        <item name="titleTextColor">@color/black</item>
        <item name="colorControlActivated">@color/color_primary</item>

    </style>

    <!-- Base application theme. -->
    <style name="AppThemLight" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="popupTheme">@style/actionMenu</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TelegramClone" parent="AppThemLight" />
</resources>